#!/bin/bash

# Specify the configuration file with all the settings:
# The argument here points to the default configuration that is used anyways
python -m src.preprocessing.extract_positions --config src/preprocessing/config.yaml